package com.java8Features.FunctionalInterface;

@FunctionalInterface
public interface FunctionalIntefaceDemo {
	void method();

	/*
	 * A functional Interface should consists of only one abstract method
	 * 
	 * We can use @FunctionalInterface annotation to make sure that we are going to
	 * create only one abstract method in the interface. If we try to add one more
	 * abstract method the compiler will throw an error as we had marked the
	 * interface with @FunctionalInterface. so commenting method2() to avoid
	 * compilation error.
	 */
	// void method2();

}
